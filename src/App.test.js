import React from 'react';
import { render } from '@testing-library/react';
import App from './App';

describe('Substitute test', () =>{
    it('Knows that racecar backwards is racecar', () =>{
        expect(2 + 2).toEqual(4);
    })
})